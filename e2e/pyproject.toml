[project]
name = "LobbyLabE2E_Tests"
version="0.0.1"
requires-python = ">=3.14"
dependencies = [
    "requests",
    "pytest",
    "websocket-client",
    "playwright",
]

[tool.pyright]
typeCheckingMode = "standard"
reportAny = false
reportExplicitAny = false