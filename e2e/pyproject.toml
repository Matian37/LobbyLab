[project]
name = "LobbyLabE2E_Tests"
requires-python = ">=3.14"
dependencies = [
    "requests",
    "pytest",
    "websocket-client",
    "playwright",
]

[tool.pyright]
typeCheckingMode = "standard"
reportAny = false
reportExplicitAny = false